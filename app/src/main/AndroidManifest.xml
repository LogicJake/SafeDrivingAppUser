<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nuaa.safedriving">

    <!-- 声明应用所需要权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="icon">
        <activity
            android:name=".activity.SignUp"
            android:label="@string/sign_up" />
        <activity
            android:name=".activity.Login"
            android:label="@string/login" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".activity.FullInEmail"
            android:label="邮箱验证" />
        <activity
            android:name=".activity.setting"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.Ride"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MyCalendar" />
        <activity android:name=".activity.ShuttleInfo" />
        <activity
            android:name=".activity.Surprise"
            android:theme="@style/AppTheme.NoActionBar.Full"></activity>
        <activity
            android:name=".activity.Startpage"
            android:theme="@style/AppTheme.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.CheckCode"
            android:label="找回密码" >
        </activity>
    </application>

</manifest>